[DEFAULT]
# Ban hosts for one hour:
bantime = 3600
# DANGER: Add scoring server IP to ignore list before enabling this, or scoring server will be banned.
ignoreip = 10.8.0.0/16 127.0.0.1/8 


# Override /etc/fail2ban/jail.d/00-firewalld.conf:
banaction = iptables-multiport

[sshd]
enabled = true