all:
  vars:
    backup_key_path: ~/.ssh/c3tbackup.pub
  children:
    ubuntu:
      hosts:
        ccdc1:
          ansible_host: ccdc1
        ccdc2:
          ansible_host: ccdc2
    centos: 
      hosts:
        wazuh:
          ansible_host: wazuh
    web_servers:
      hosts:
        ccdc1:
        ccdc2:
    wazuh_servers:
      hosts:
        wazuh:
    jenkins_servers:
      hosts:
    docker_swarm_servers:
      hosts:
        ccdc1: 
        ccdc2:
    honeypots:
      hosts:
        ccdc1:
        ccdc2:
    # Add hosts that require a password to login for scoring checks    
    requires_password_ssh:
      hosts:

