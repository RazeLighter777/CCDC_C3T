all:
  vars:
    backup_key_path: ~/.ssh/c3tbackup.pub
  children:
    ubuntu:
      hosts:
        ccdc1:
          ansible_host: ccdc1
        ccdc2:
          ansible_host: ccdc2
    centos: 
      hosts:
        wazuh:
          ansible_host: wazuh

